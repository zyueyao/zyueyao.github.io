// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Deployed via GitHub Pages as a user site (served at the domain root).
	site: 'https://zyueyao.github.io',
});
